# Conventions

The following conventions are used when writing the library. To ensure compatibility to future versions, you as an user
should also follow them:

* All declarations in headers of this library start with `nt_` or `NT_`.
* Private declarations are prefixed with `P_` and should never be used directly by the end user.
